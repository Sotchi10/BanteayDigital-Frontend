VITE_BACKEND_URL=http://localhost:3000
# Optional for deployments that call the backend directly instead of using the
# Vite development proxy. Include the /api prefix, for example:
# VITE_API_BASE_URL=https://api.example.com/api
